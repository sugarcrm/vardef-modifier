<?php

/**
 * Please note that this file has been generated based a file located on this path:
 * {{ fileName }}
 * and may be overwritten at a later point..
 */

{% for field in fields %}
{% if field.isChange %}
{% for key,value in field.def %}
$dictionary['{{ dictionaryKey }}']['fields']['{{ field.name }}']['{{ key }}'] = {{ value | raw }};
{% endfor %}
{% else %}
$dictionary['{{ dictionaryKey }}']['fields']['{{ field.name }}'] = {{ field.def | raw }};
{% endif %}

{% endfor %}
{% for relationship in relationships %}
$dictionary['{{ dictionaryKey }}']['relationships']['{{ relationship.name }}'] = {{ relationship.def | raw }};

{% endfor %}
{% for index in indices %}
$dictionary['{{ dictionaryKey }}']['indices']['{{ index.name }}'] = {{ index.def | raw }};

{% endfor %}
{% for check in duplicate_check %}
$dictionary['{{ dictionaryKey }}']['duplicate_check']['{{ check.name }}'] = {{ check.def | raw }};

{% endfor %}
{% for acl in acls %}
$dictionary['{{ dictionaryKey }}']['acls']['{{ acl.name }}'] = {{ acl.def | raw }};

{% endfor %}

{% for vis in visibility %}
$dictionary['{{ dictionaryKey }}']['acls']['{{ vis.name }}'] = {{ vis.def | raw }};

{% endfor %}
